#
# Kenya Procurement - Python package metadata
#
# Keep the version in sync with `setup.py` / `pyproject.toml`. Frappe reads
# `__version__` for `bench --version` style output and to record the app
# version on the doctype Version table.
#
__version__ = "0.1.0"